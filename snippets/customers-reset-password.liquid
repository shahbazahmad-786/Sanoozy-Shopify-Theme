<div class="sanoozy-account--narrow">
  {% render 'account-logo' %}

  <div class="sanoozy-account-card">
    <h1>Reset password</h1>
    <p>Choose a new password for your Sanoozy account.</p>

    {%- form 'reset_customer_password', class: 'sanoozy-account-form' -%}
      {%- if form.errors -%}
        <div class="sanoozy-account-alert sanoozy-account-alert--error">
          {{ form.errors | default_errors }}
        </div>
      {%- endif -%}

      <label class="sanoozy-account-field">
        <span>New password</span>
        <input
          type="password"
          name="customer[password]"
          autocomplete="new-password"
          required
          placeholder="New password"
        >
      </label>

      <label class="sanoozy-account-field">
        <span>Confirm password</span>
        <input
          type="password"
          name="customer[password_confirmation]"
          autocomplete="new-password"
          required
          placeholder="Confirm password"
        >
      </label>

      <button type="submit" class="button">Update password</button>
    {%- endform -%}
  </div>
</div>
